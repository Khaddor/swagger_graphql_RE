
// graphql/resolvers.js

// Example resolver using MongoDB
const Annonce = require('../models/annonces');
const resolvers = {
  /*
  Query: {
    getAnnouncements: async () => {
      return await Annonce.find();
    },
  },
   */
};

module.exports = resolvers;
